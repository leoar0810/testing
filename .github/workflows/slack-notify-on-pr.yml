name: Notify Slack on Pull Request

on:
  pull_request:
    types: [opened]

jobs:
  notify-slack:
    runs-on: ubuntu-latest

    steps:
      - name: Send Slack Notification with curl
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{
              "text": "Un nuevo PR ha sido creado por: *${{ github.actor }}* :rocket:",
              "attachments": [
                {
                  "color": "#36a64f",
                  "title": "Pull Request URL",
                  "title_link": "${{ github.event.pull_request.html_url }}",
                  "fields": [
                    {
                      "title": "Título",
                      "value": "${{ github.event.pull_request.title }}",
                      "short": true
                    },
                    {
                      "title": "Desarrollador:",
                      "value": "${{ github.actor }}",
                      "short": true
                    }
                  ],
                  "fields": [
                    {
                      "title": "Pull Request URL:",
                      "value": "${{ github.event.pull_request.html_url }}",
                      "short": true
                    },
                    {
                      "title": "Commit:",
                      "value": "${{ github.actor }}",
                      "short": true
                    }
                  ]
                }
              ]
            }' \
            ${{ secrets.SLACK_WEBHOOK_URL }}